[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build -w @rankflow/frontend"

[deploy]
startCommand = "npm run start -w @rankflow/frontend"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
